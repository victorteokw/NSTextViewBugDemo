To reproduce the bug:

1. Build and run this project.
2. Type a lot of "return"s, to insert many new lines. After the new lines exceeds the window. Starting some time point, the NSTextView starts to jump while typing.
